{% extends 'base.html.twig' %}

{% block title %}ChatChannel{% endblock %}

{% block body %}
    <h1>ChatChannel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chat_channel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chat_channel.name }}</td>
            </tr>
            <tr>
                <th>Visibility</th>
                <td>{{ chat_channel.visibility }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chat_chat_channel_index') }}">back to list</a>

    <a href="{{ path('app_chat_chat_channel_edit', {'id': chat_channel.id}) }}">edit</a>

    {{ include('chat/chat_channel/_delete_form.html.twig') }}
{% endblock %}
